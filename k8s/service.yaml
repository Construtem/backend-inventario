apiVersion: v1
kind: Service
metadata:
  name: backend-inventario-service
spec:
  selector:
    app: backend-inventario
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
